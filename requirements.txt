# Python Knowledge Base — dependencies
# Core dependencies are stdlib-only for docs/ and src/ chapters 01-25.
# The following are used by later chapters (web, database, api, ML) and mini-projects.

# Testing
pytest>=8.2.0
pytest-cov>=5.0.0

# Web / API (chapters 27, 29 + projects: weather_app, rest_api_fastapi)
requests>=2.32.0
fastapi>=0.111.0
uvicorn>=0.30.0
beautifulsoup4>=4.12.0

# Data (chapters 24, 30 + projects: csv_analyzer, expense_tracker)
pandas>=2.2.0

# Type checking / lint (chapter 19)
mypy>=1.10.0
ruff>=0.5.0

# Machine learning (chapter 30)
scikit-learn>=1.5.0
numpy>=1.26.0
